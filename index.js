import React from "react";
import ReactDOM from "react-dom";
import DiarioMural from "./DiarioMural";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DiarioMural />
  </React.StrictMode>
);
